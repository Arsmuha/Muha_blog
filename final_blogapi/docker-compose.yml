version: "3.9"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:////app/data/blog.db
      - SECRET_KEY=change-me
    volumes:
      - blog_data:/app/data
      - uploads:/app/static/uploads
volumes:
  blog_data:
  uploads:
